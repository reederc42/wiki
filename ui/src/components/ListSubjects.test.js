import { describe, test } from "node:test";

describe("List Subjects component", () => {

    test("creating element updates subjects");
    test("shows all subjects in links");
    test("subjects are split into n columns");
});
