import { describe, beforeEach, after, test } from "node:test";
import assert from "node:assert";
import { DOM } from "../test-helpers/dom.js";

describe("subjects store", () => {
    test("update from api");
    test("")
});
